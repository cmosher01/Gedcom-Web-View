@template person(person, file, otherFiles, footnotes, root)@

@include header()@

@include head(person, root)@

<body>
    @include nav(file, otherFiles, person)@

    <article class="person">
        <section class="parents">
            <div class="father">
                <span class="relation">father:</span>
                @include linkUp(person.getFather())@
            </div>
            <div class="mother">
                <span class="relation">mother:</span>
                @include linkUp(person.getMother())@
            </div>
        </section>
        <header>
            <hr class="sim">
            <h1 class="personName">@nu.mine.mosher.Util.esc(person.toString())@</h1>
        </header>

        @include events(person.getEventsWithDittoedPlaces(), footnotes, person.isPrivate())@

        @include partnerships(person.getPartnerships(), footnotes, person.isPrivate())@

        @include footnotes(footnotes)@
    </article>
</body>

@include footer()@
