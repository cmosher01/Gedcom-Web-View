@template eventsSelf(rEvent, footnotes)@

@if (!rEvent.isEmpty())@
    <table class="events">
        <tbody>
            @loop iEvent : rEvent.size()@
                @include event(rEvent[iEvent], footnotes)@
            @end loop@
        </tbody>
    </table>
@end if@
